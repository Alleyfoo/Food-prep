# Handoff — food-prep

## State
Round 10 complete and pushed (b1e95ff..f8bb08d on main). Broccoli added as
the fifth full ingredient — earning its place by modelling a genuinely
NEW food state (steam), per the user's hedgerow rule. 83 tests passing.
Remote: https://github.com/Alleyfoo/Food-prep.

## What was completed this session (round 10 — broccoli, option C)
Broccoli is a brassica like cabbage, so the whole point was to add a NEW
food state rather than re-tread roast/simmer. The new state is STEAM:
brief wet heat that locks bright green + tender-crisp; the doneness cliff
(perfect → drab sulfurous mush in seconds) is encoded as a RISK, mirroring
the cabbage guardrail.

### Data (`src/foodprep/data/tomato.yaml`)
- NEW technique `steam` (wet heat, moisture gain, not preservation) —
  genuinely absent from the vocabulary before; broccoli-only so far.
- NEW texture tag `tender_crisp` — the steamed/blanched doneness marker.
- NEW evidence source `steam_culinary` (steam + shock color/texture lock).
- NEW ingredient `broccoli` (kind: full, FI availability, aliases incl.
  parskaali/broccolini).
- NEW components: raw_broccoli_component, steamed_broccoli_component,
  roasted_broccoli_component, stir_fried_broccoli_component,
  broccoli_soup_base.
- 5 transformations: raw_assemble (harsh_when_raw), steam, roast,
  stir_fry, soup (all heat ones carry sulfurous_if_overcooked as a RISK,
  never a missing role). Each has tags, missing_roles, uses, evidence.
- Broccoli pairings: Cook-mode fillers for every transformation's missing
  roles (≥25), plus 4 Nordic-plausible Scout/experimental pairings
  (lingonberry_vinegar, walnut, brown_butter, smoked_yogurt).

### Tests (`tests/test_query.py`)
+8 round-10 acceptance tests: broccoli loaded as full; the five
transformations; each has missing_roles; risks-are-risks-not-roles
(sulfur/harsh never in role names; raw→harsh_when_raw; all heat→
sulfurous_if_overcooked; risk string absent from missing roles); steam
is broccoli-only + carries the tender_crisp tag; Cook/Scout split
(lingonberry/walnut excluded from Cook fillers_by_role, present in
scout_rows); branch render shows steam + the risk. Total transformation
count assert moved 33 → 38 (12+4+9+8+5).

### Smoke verification
- In-memory build: no LoadError; all 5 broccoli transformations present.
- `query.branch_card(conn,'broccoli','steam')`: component
  steamed_broccoli_component; tags incl tender_crisp; risks
  sulfurous_if_overcooked; missing salt/fat/acid/protein/carb; Cook
  fillers_by_role resolves all five roles (no experimental leaking in).
- `query.scout_rows(ingredient='broccoli')`: 4 experimental pairings.
- `pytest -q` → 83 passed.
- Screenshots re-captured (broccoli added a dropdown option) + PIL-verified.

## Commits this session
- `4719c76` feat: broccoli — fifth full ingredient modelling a new steam food state
- `f8bb08d` chore: refresh screenshots after broccoli dropdown addition

## Uncommitted changes
None. Working tree clean (aside from this handoff file).

## Important constraint (do not violate)
The model in this environment has **no image reading** (vision). Never ask
it to inspect a PNG — it crashes. Verify screenshots with PIL
(`Image.open(p).verify()`). Screenshots are re-captured via Playwright
only; never visually inspected.

## The hedgerow rule (the user's standing constraint)
"Not every ingredient needs the tomato treatment; that is how the
ontology becomes a hedgerow." A new full ingredient must model a
genuinely NEW food state. Broccoli passed via `steam` + `tender_crisp`.
Carrots/mushrooms were explicitly deferred; if revisited, identify the new
state first (e.g. mushroom: dry/concentrate + umami deepening; carrot:
sweet-concentrate via roast/caramelise + raw-shredded slaw) before
authoring — do NOT just add roast/simmer duplicates.

## Recommended next task
The user's post-presentation menu had A/B/C/D. C (broccoli) is done.
Remaining candidates:
- **A. Small pantry selector** — "What do I have?" filters fillers/components.
- **B. Screenshot/demo export** — export a plate-balance result as Markdown.
- **D. Review queue** — for future recipe/corpus extraction candidates.
- (C is now complete; do not add another ingredient without a new state.)

Standing engine backlog (lower priority):
- Curate more `curated_role_fit` annotations (still only on 2 pairings;
  broccoli pairings are not yet annotated — could annotate the strongest
  ones, e.g. broccoli+soy_sauce/salt in stir_fry).
- Scout-from-rarity: derive experimental pairings from co-occurrence +
  FlavorDB shared compounds instead of hand-authoring (broccoli Scout
  pairings are hand-authored like the rest).
- Plate-condition integration: Plate Balance prefers fillers whose
  `repairs` match the plate's condition (heavy/dry/acidic); pin the
  condition vocabulary first.

## Data sources
- `F:\download\google\CulinaryDB` — 45,773 recipes / 931 ingredients / 456k
  links; backfill keys by Entity ID. Used as evidence only.
- `F:\download\google\deep-research-report (33).md` — the design source.
- `C:\Users\pertt\parcel-ops` — Streamlit layout/design language copied to
  save time (per the user's instruction).