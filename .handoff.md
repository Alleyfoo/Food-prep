# Handoff — food-prep

## State
Round 9 complete and pushed (7dc2f36..16d5d5b on main). The presentation
pass is done: README rewritten around the five tabs, the demo made
robust (UTF-8 + out-of-the-box auto-build). No engine expansion, no new
ingredient (per the user's explicit instruction). 76 tests passing.
Remote: https://github.com/Alleyfoo/Food-prep.

## What was completed this session (round 9 — presentation pass)
- **README rewrite** (`README.md`):
  - New "Why this is not a recipe app" lead section — the
    does-not-generate-recipes / four-questions block (the user's most
    important section).
  - "The concept in 60 seconds" — the five demo flows mapped to the five
    Streamlit tabs, runnable via `foodprep demo`.
  - Mermaid architecture diagram (YAML → loader → SQLite → query →
    CLI + UI) + "one truth, three faces" note.
  - Known limitations section (hand-authored not learned; sparse
    curated_role_fit; no per-component risk aggregation; build-time DB
    cache; pairing keying quirk; Playwright requirement).
  - Web UI section expanded with the Ingredient Foundry header copy and
    the Scout "not classics / taste before serving" copy.
  - Layout block updated to list demo.py, scripts/, docs/screenshots/.
- **Demo robustness** (`src/foodprep/demo.py`, `src/foodprep/cli.py`):
  - `run_demo` reconfigures stdout to UTF-8 (guarded) so the box-drawing
    banners (═, →) don't `UnicodeEncodeError` on Windows cp1252.
    `scripts/demo.py` bypassed `cli.main` (which already reconfigured),
    so it crashed; now covered.
  - `cmd_demo` builds in-memory if the DB is empty instead of erroring —
    `foodprep demo` now works on a fresh clone with no prior
    `foodprep build`. No file is written.

### Smoke verification
- `python scripts/demo.py` → exit 0, all 5 flows print.
- `foodprep demo` (via main(['demo']) on a clone with no db file) →
  auto-builds in memory, prints all 5 flows, no stray foodprep.db.
- `pytest -q` → 76 passed.
- README flows cross-checked against live demo output (text only; the
  model here has no image-reading capability, so screenshots are never
  visually inspected — only PIL file-integrity checks).

## Commits this session
- `223ab17` fix: demo runs out of the box (UTF-8 stdout + in-memory auto-build)
- `16d5d5b` docs: README presentation pass — rewrite around the five tabs

## Uncommitted changes
None. Working tree clean (aside from this handoff file until committed).

## Important constraint (do not violate)
The model used in this environment **does not support image reading** (no
vision). Any step that tries to have the model visually inspect a PNG
must be **skipped** — it will crash/hang. Verify screenshot file integrity
with PIL (`Image.open(p).verify()`) instead. Recorded so future sessions
don't repeat the crash.

## Round-8 presentation checklist (now all done)
1. ✅ Screenshots — `scripts/capture_screenshots.py` + 5 PNGs in README
2. ✅ README rewritten around the five tabs
3. ✅ "Why this is not a recipe app" section
4. ✅ Short demo script — `foodprep demo` / `scripts/demo.py`
5. ✅ Example prompts / UI flows — the five demo flows in README + demo.py
6. ✅ Architecture diagram (mermaid)
7. ✅ Known limitations section

## Recommended next task (the user's post-presentation menu)
The user listed four options to consider *after* the demo is clean. The
demo is now clean. Pick one (the user has not chosen yet):
- **A. Small pantry selector** — "What do I have?" filters fillers/components.
- **B. Screenshot/demo export** — export a plate-balance result as Markdown.
- **C. Add carrots or mushrooms** — a new full ingredient only now that the
  demo is clean (still subject to the hedgerow rule: only if a new food
  state is genuinely modelled).
- **D. Review queue** — for future recipe/corpus extraction candidates.

Plus the standing engine backlog (lower priority than A–D above):
- Curate more `curated_role_fit` annotations (highest-value, lowest-risk).
- Scout-from-rarity: derive experimental pairings from co-occurrence +
  FlavorDB shared compounds instead of hand-authoring.
- Plate-condition integration: let Plate Balance prefer fillers whose
  `repairs` match the plate's condition (heavy/dry/acidid); pin the
  condition vocabulary first.

## Data sources
- `F:\download\google\CulinaryDB` — 45,773 recipes / 931 ingredients / 456k
  links; backfill keys by Entity ID. Used as evidence only.
- `F:\download\google\deep-research-report (33).md` — the design source.
- `C:\Users\pertt\parcel-ops` — Streamlit layout/design language copied to
  save time (per the user's instruction).