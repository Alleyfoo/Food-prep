# Handoff — food-prep

## Completed

Implementation of the new product roadmap has started with two pushed
milestones:

- `9316285` — `docs: define cook and scout product roadmap`
  - Added `docs/PRODUCT_DESIGN.md`.
  - Added `docs/ROADMAP.md`.
  - Added ADR `docs/adr/0001-dual-mode-cook-scout.md` establishing Cook and
    Scout as equal product modes and the current UI as the Workshop.
- `6b66229` — `feat: add shared culinary vocabulary`
  - Added packaged `src/foodprep/data/vocabularies.yaml` covering preparations,
    transformation categories, flavours, textures, corrections, destinations,
    confidence, and uncertainty.
  - Added immutable vocabulary loading, lookup, and validation in
    `src/foodprep/vocabulary.py`.
  - `loader.build()` validates vocabulary before rebuilding SQLite so invalid
    vocabulary data cannot wipe an existing local database.
  - Added five vocabulary tests, including duplicate and unknown-value guards.

Verification: `py -3.11 -m pytest -q` → 103 passed.

Both commits were pushed to `origin/main`.

## Uncommitted changes

- `Development` and `system-design.md` remain untracked. They are the user's
  source documents and were intentionally preserved without staging or editing.

## Known issues

- The previously documented Streamlit Cloud visual-polish gap remains deferred.
  Do not treat it as part of the journey-model implementation.
- The controlled vocabulary is validated and queryable, but existing ontology
  tags and techniques have not yet been migrated to reference it. That linkage
  should happen incrementally with the journey model rather than through a
  destructive rewrite.

## Recommended next task

Implement the first complete broccoli journey slice:

1. Define additive journey/state-transition structures without removing current
   transformations or component profiles.
2. Model the four agreed broccoli paths: stir-fried stems, roasted florets,
   steamed-and-dressed florets, and roasted broccoli crushed into a sauce/spread.
3. Add a journey query endpoint and text-first CLI rendering.
4. Add four causal end-to-end tests covering preparation, sensory changes,
   flavour direction, risks/corrections, transitions, and destinations.

Keep current UI, query, export, and all 103 tests working. Do not expand the
ingredient catalogue or build the new consumer UI yet.

## Standing constraints

- Do not stage CI-guarded files without explicit confirmation:
  `.github/workflows/ci-full.yml`, `scripts/ci_build_smoke.py`,
  `scripts/smoke/ci_gui_smoke.py`, and
  `scripts/checks/check_update_correctness_signatures.py`.
- Use `available_*`, never “pantry,” in product terminology.
- Markdown export must serialize computed dictionaries only.
- A new full ingredient must introduce a genuinely new food state.
