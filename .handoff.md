# Handoff — food-prep

## Completed

Scout hypothesis generation is implemented and pushed:

- `f6ebc21` — `feat: generate scout hypotheses from analogies`
  - Added reusable `analogy_rules`; these are generator inputs, not stored final
    candidate pairings.
  - Added structured compatibility mechanisms, shared functions, meaningful
    differences, state requirements, expected risks, confidence, and templated
    explanations.
  - Added `generate_scout_hypotheses()` with accepted and rejected rule paths.
  - Compatibility ranking uses state evidence only. Novelty is returned as
    `not_checked` and never enters compatibility classification.
  - Added `foodprep hypotheses <component>` CLI output.
  - Existing curated `foodprep scout` output remains unchanged for compatibility.
  - Added tests proving `rare` and `not_observed` cannot rescue a candidate with
    zero compatibility evidence.

Verification: `py -3.11 -m pytest -q` → 128 passed.

The commit was pushed to `origin/main`.

## Generated proof

`foodprep hypotheses roasted_broccoli_component` currently generates:

- roasted broccoli + brown butter via fat substitution and toasted-flavour
  reinforcement;
- roasted broccoli + lingonberry vinegar via acidic analogue substitution from
  lemon and contrast with browned sweetness.

Both show compatibility, analogy, meaningful difference, risk, and the honest
statement `novelty: not checked — no corpus claim yet`.

## Uncommitted changes

- `Development` and `system-design.md` remain untracked user source documents.
  They were intentionally not staged or edited.

## Known issues

- The checked-in `foodprep.sqlite` must be rebuilt to receive additive tables.
- Novelty observations do not exist yet; generated candidates deliberately make
  no rarity claim.
- Scout rules currently use direct candidate substitutions. Future expansion
  can generate wider candidate sets from shared functional profiles, but should
  retain transparent rejection reasons.
- Streamlit Cloud visual polish remains deferred.

## Recommended next task

Implement corpus-scoped novelty observations for generated hypotheses:

1. Add corpus metadata and candidate novelty observation tables.
2. Reuse the existing local CulinaryDB ingestion/alias machinery where possible.
3. Record direct co-occurrence, distinct contexts, corpus scope, search date,
   and coverage state.
4. Distinguish `not_observed` from `insufficient_coverage`.
5. Attach novelty to generated hypotheses only after an explicit observation;
   never modify compatibility scores or candidate classes from rarity alone.

After novelty, add the controlled tasting protocol and result-recording loop.

## Standing constraints

- Do not stage CI-guarded files without explicit confirmation:
  `.github/workflows/ci-full.yml`, `scripts/ci_build_smoke.py`,
  `scripts/smoke/ci_gui_smoke.py`, and
  `scripts/checks/check_update_correctness_signatures.py`.
- Use `available_*`, never “pantry,” in product terminology.
- Markdown export must serialize computed dictionaries only.
- Do not expand full ingredients without a genuinely new food state.
