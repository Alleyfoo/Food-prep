"""Food-prep Streamlit UI package.

The app lives in ``streamlit_app.py``; run with ``streamlit run app.py``
(root launcher) or ``streamlit run src/foodprep/ui/streamlit_app.py``.
Importing this package does NOT import streamlit, so core tests stay light.
"""